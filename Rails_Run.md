# Running a Rails App

#### Install Project and database

```bash
# cd to [project directory]/huddle
bundle install

# setup database
rake db:create
rake db:migrate
```

#### Run Project on server

```bash
bundle exec rails s
```
